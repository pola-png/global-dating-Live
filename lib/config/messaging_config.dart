class MessagingConfig {
  /// Topic ID configured in Appwrite Messaging for app-wide notifications.
  ///
  /// Create a topic in the Appwrite console (e.g. "global-notifications")
  /// and paste its ID here.
  static const String globalTopicId = 'global-notifications';
}

