class MessagingSubscriptionService {
  /// Subscribe a user to the global topic for app-wide notifications (Stubbed for Supabase)
  static Future<void> subscribeToGlobal({
    required String userId,
    required String targetId,
  }) async {
    // Stubbed for Supabase. Push notifications can be integrated using FCM / Supabase Edge Functions.
  }
}
