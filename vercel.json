{
  "github": {
    "silent": true
  },
  "buildCommand": "export PATH=\"$PATH:$PWD/flutter/bin\" && flutter build web --release --web-renderer canvaskit",
  "outputDirectory": "build/web",
  "installCommand": "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter doctor && flutter pub get"
}
